export default function Layout({ children }) {
  return (
    <html lang="en">
      <head>
        <title>Next.js || Material UI Demo</title>
      </head>
      <body>{children}</body>
    </html>
  )
}
